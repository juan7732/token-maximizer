# kb-opinionated-cli

CLI design patterns distilled from real codebases I trust. Pull in when building or refactoring a CLI tool.

## Contents

- `patterns-from-ergo.md` - workspace-manager CLI patterns: safety rails, TUI fallback, exec seams, self-update, integration harness

## When to pull into a workspace

Any project with a CLI surface. Skip it elsewhere.
