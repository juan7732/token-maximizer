# [Pattern or Practice Name]

**Category:** [CLI Design | Agent Practices | Frontend | ...] · **Source:** `github.com/owner/repo` @ `<short-hash>` · **Captured:** YYYY-MM-DD

<!-- One or two lines: what this file holds and how binding it is. Captures are
defaults with reasons; state when to deviate. -->

## [Pattern name, one per section]

**[Claim as a bold lead sentence.]** Plain prose explaining the choice and the reason it works. Include the failure the pattern prevents, if there is one.
Instead of: the conventional alternative this rejects. <!-- omit if the alternative is not informative -->
Evidence: `path/to/file.go:NN`, `path/to/other.go:NN`. <!-- paths valid at the pinned commit; if the source repo cannot be pinned or will not be available, drop the evidence entirely -->

## Observed result

<!-- Real outcome only, cited from where it was recorded. No invented metrics. -->

## See also

<!-- Related kb files, the executable skill if one exists. If a skill is the
source of truth, say so and do not duplicate its mechanics here. -->

<!-- Style rules (also in AGENTS.md): plain sentences, no "not just X, it's Y"
constructions, no em-dashes, cut filler. A capture earns its place only if it is
opinionated, non-obvious to a strong model, and transferable. -->
